// Minimal Zig wrapper to build the C library
// This file is just a placeholder - the actual C code is compiled via addCSourceFile
const std = @import("std");

pub fn build(b: *std.Build) void {
    _ = b;
}

